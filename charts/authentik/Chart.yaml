apiVersion: v2
name: authentik
description: Authentik Identity Provider for centralized authentication
type: application
version: 1.0.0
appVersion: "2024.10.0"

dependencies:
  - name: authentik
    version: 2024.10.0
    repository: https://charts.goauthentik.io
