apiVersion: v1
kind: Secret
metadata:
  name: authentik-postgresql
  namespace: {{ .Release.Namespace }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
stringData:
  # Fixed password - matches PostgreSQL database configuration
  # DO NOT use randAlphaNum as it regenerates on every upgrade!
  password: "WNAkt8ZouZRhvlcf3HSAxFXQfbt4qszs"
